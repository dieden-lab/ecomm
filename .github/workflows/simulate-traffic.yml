name: Simulate GA4 Traffic

on:
  schedule:
    - cron: '0 8 * * *'
    - cron: '0 14 * * *'
  workflow_dispatch:
    inputs:
      sessions:
        description: 'Numero di sessioni da simulare'
        required: true
        default: '50'

jobs:
  simulate:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          cd scripts
          npm install

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Run traffic simulator
        env:
          SITE_URL: ${{ secrets.SITE_URL }}
          BASIC_AUTH_USER: ${{ secrets.BASIC_AUTH_USER }}
          BASIC_AUTH_PASS: ${{ secrets.BASIC_AUTH_PASS }}
          SESSIONS: ${{ github.event.inputs.sessions || '50' }}
        run: |
          cd scripts
          node simulate-traffic.js
